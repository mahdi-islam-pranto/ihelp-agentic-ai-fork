validation_prompts = {
    "name_validation_prompt": '''Extract only name from the user input. If there is no name, return 'Unknown'. Ex: my name is John, return John. or I'm John, return John. User can provide their full name, first name, last name, nick name, etc. Return the name as it is provided by the user. Remember you will return only the name not any other text. Otherwise return only 'Unknown'.''',
    "age_validation_prompt" : '''Extract only age from the user input. If there is no age, return 'Unknown'. Ex: I'm 10 years old/i'm 10/my age is 10/etc, return 10. User can give age in words, ex: twenty five, return 25. Remember you will return only the age (number) not any other text. Otherwise return only 'Unknown'.''',
    "date_of_birth_validation_prompt" : '''Extract only date of birth from the user input. If there is no date of birth, return 'Unknown'. Ex: I'm born on 1st January 2000, return 1st January 2000. or I'm born on 01/01/2000, return 01/01/2000. User can provide their date of birth in any format. Return the date of birth as it is provided by the user. Remember you will return only the date of birth not any other text. Otherwise return only 'Unknown'.'''
}